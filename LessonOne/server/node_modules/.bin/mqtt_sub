#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mqtt/bin/mqtt_sub" "$@"
  ret=$?
else 
  node  "$basedir/../mqtt/bin/mqtt_sub" "$@"
  ret=$?
fi
exit $ret
