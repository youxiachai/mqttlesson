#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mqtt/bin/mqtt_pub" "$@"
  ret=$?
else 
  node  "$basedir/../mqtt/bin/mqtt_pub" "$@"
  ret=$?
fi
exit $ret
